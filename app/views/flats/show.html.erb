<h1><%= @flat.name %></h1>
<p><%= @flat.description %></p>

<% if user_signed_in? %>
  <%= form_with(model: [@flat, Booking.new], local: true,
    data: { controller: "booking", action: "submit->booking#confirmBooking" }) do |form| %>

    <div>
      <%= form.label :start_date %>
      <%= form.date_field :start_date %>
    </div>
    <div>
      <%= form.label :end_date %>
      <%= form.date_field :end_date %>
    </div>
    <%= form.submit "Request Booking", class: "btn btn-primary" %>
  <% end %>
<% end %>

<!-- 🔙 Back to Home Button -->
<div style="margin-top: 20px;">
  <%= link_to "Back to Home", root_path, class: "btn btn-secondary" %>
</div>

<!-- 🔙 Return to Flat List Button -->
<div style="margin-top: 20px;">
  <%= link_to "Return to Flat List", flats_path, class: "btn btn-secondary" %>
</div>
